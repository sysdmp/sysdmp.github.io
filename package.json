{
  "name": "ddda-build-solver-web",
  "version": "2.2.0",
  "description": "Browser MILP solver for Dragon's Dogma: Dark Arisen level-200 builds (uses HiGHS/WASM).",
  "license": "MIT",
  "type": "module",
  "main": "src/solver.js",
  "scripts": {
    "test": "node src/test.mjs && make && node src/test-dom.mjs && npm run test:lint",
    "test:dom": "node src/test-dom.mjs",
    "test:py": "node src/test-python.mjs",
    "test:lint": "npm run test:lint:py && npm run test:lint:js",
    "test:lint:py": "uvx ruff check pycli",
    "test:lint:js": "npx --yes oxlint@1 --deny-warnings src",
    "build": "make"
  },
  "dependencies": {
    "highs": "^1.14.2"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "jsdom": "^29.1.1"
  }
}
